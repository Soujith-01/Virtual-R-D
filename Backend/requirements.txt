# ---------------------------------------------------------------------------
# Virtual R&D Lab - core backend requirements
# (already sufficient to run the full demo end-to-end)
# ---------------------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9

# --- machine learning -------------------------------------------------------
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# --- optional LLM provider (only needed for `requests`-based providers) -----
requests>=2.31

# NOTE: the retrieval stack (Sentence Transformers + FAISS) lives in
# `requirements-rag.txt` because it pulls in PyTorch (~2 GB). The RAG layer
# auto-detects it and falls back to a scikit-learn TF-IDF embedder when absent,
# so the hackathon demo works without the download.
