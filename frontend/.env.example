# Backend base URL. Copy this file to `.env` and adjust if your API runs elsewhere.
# The default (http://127.0.0.1:8000) matches `uvicorn main:app` run from backend/.
VITE_API_URL=http://127.0.0.1:8000
